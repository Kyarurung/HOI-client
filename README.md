# HOI-client

`dev.hoi.client.HoiClient`는 Fabric 진입점을 유지합니다. 구현은 `research`(연구 화면·트리), `screen`(국가·산업·첩보·팝업 화면), `ui`(공통 버튼·HUD·아트), `map`(지도 장면), `input`(단축키·지도 클릭·이동), `audio`(음악·효과음), `mixin`으로 나눕니다. 공통 진행 버튼은 `ui.PanelButton`입니다. 단위 테스트와 렌더링 검사는 해당 기능 패키지에 두고, `ResearchScreenGameTest`가 렌더링 검사의 진입점을 유지합니다. 이 패키지 이동은 `hoi:` 통신 ID나 저장 데이터 형식을 바꾸지 않습니다.

Minecraft **26.2 · Fabric · Java 25**용 HOI 전용 클라이언트입니다. 자국 현황, 연구, 정보기관과 지휘관 상세를 표시하며 연구·첩보 조작은 인증된 서버 API로 요청합니다.

정치 화면의 용어는 `세력`, `경제-정치 연합`을 사용하며 `선거 실시` 라벨은 표시하지 않습니다. 경제 모델·세력·경제-정치 연합·정치 체제 칸은 이름이나 수치 없이 이미지만 표시합니다. 빈 지도자·국가 중점과 정치 분류 아이콘은 외부 팩의 `politics/empty/` 원본 이미지를 사용합니다. 팩에 보관된 31개 국가·세력의 전체 인물·중점·정신 이미지가 곧 서버의 현재 지도자 선정이나 모든 루트의 실행 구현을 의미하지는 않습니다.

지도자·국가 중점·국민정신의 상단 제목 라벨은 생략하고 하단 구역 제목은 유지합니다. 지도자 초상화와 이름은 하나의 칸 안에 배치하고 이름은 하단의 TFR 원본 연두색 이름판 위에 중앙 정렬합니다. 국가 중점은 `[이미지 | 국가 중점 선택]` 순서로 배치하며 오른쪽 제목 영역만 클릭할 수 있습니다. 선택 안내는 흰색, 진행 중인 중점 제목은 `<gold>`로 중앙에 표시하며 배경과 버튼 테두리는 TFR 원본 이미지를 사용합니다. 다음 선거는 서버 날짜를 `다음 선거 5월 2032`처럼 표시하고 날짜에 `<gold>` 색상을 적용합니다. 선거 영역 아래 여백과 실제 클릭 영역도 함께 맞춥니다.

정치 상단 이미지는 각 칸의 중앙에 비율을 유지해 배치하되 국민정신 줄은 왼쪽부터 배치합니다. 지도자 영역은 기존 너비보다 8 GUI 단위 넓히되 전체 패널 너비는 유지합니다. 세부 이념 항목이 전송되면 통치 형태 대신 그 이름을 표시하고 호버로 설명을 보여줍니다. 현재 서버는 세부 이념을 추적하지 않으므로 일반 통치 형태를 유지하며, 원본 시작 이념이나 스크린샷의 특정 이념을 현재 국가 상태로 대신 표시하지 않습니다.

국민정신 아이콘 줄은 휠과 가로 휠로 끝까지 스크롤하며, 개별 아이콘 호버로 서버가 보낸 효과와 설명을 표시합니다. 지도자 초상화와 이름은 클릭 동작이나 강조 없이 같은 설명 툴팁만 표시합니다. 긴 툴팁은 Shift+휠로 읽습니다. 지도자 설명이 없는 스냅샷에는 임의의 인물·특성을 채우지 않습니다. 현재 서버의 지도자 필드는 비어 있으며, 국민정신은 등록된 실제 보정과 제공된 설명만 전송합니다. 렌더링 검증의 인물과 효과는 테스트 데이터이며 실제 캠페인 상태가 아닙니다. 국민정신 왼쪽은 이념 이미지 칸이며 비어 있어도 국민정신 호버·스크롤 영역에 포함되지 않습니다.

| 저장소 | 역할 |
|---|---|
| [HOI-client](https://github.com/Kyarurung/HOI-client) | 이 저장소. 클라이언트 모드 소스·빌드·UI 테스트 |
| [HOI-server](https://github.com/Kyarurung/HOI-server) | 서버 실행 파일·모드·설정이 들어가는 운영 폴더 |
| [HOI](https://github.com/Kyarurung/HOI) | 서버 모드 소스·전략 시뮬레이션·게임 콘텐츠·위키 참고 문서 |
| [HOI-resourcepack](https://github.com/Kyarurung/HOI-resourcepack) | 비공개 원본·PNG 자료와 실행용 UI·지도·모델 리소스팩 |

## 설치

1. 플레이어의 Minecraft 26.2에 Fabric Loader 0.19.5 이상과 Fabric API 0.159.0+26.2를 설치합니다.
2. `hoi-client-0.1.0-SNAPSHOT.jar`를 플레이어의 `mods/` 폴더에 넣습니다.
3. HOI 서버에 접속하여 `/hoi select`로 국가를 선택합니다.
4. `/hoi ready`로 전원 준비를 마치면 게임이 일시정지 상태로 시작합니다. **Shift+F** 또는 `/hoi menu`로 국가 메뉴를 엽니다. 연구는 메뉴의 연구 아이콘, `/hoi research` 또는 `/hoi-research`로 엽니다. R 연구 단축키는 제거했으며 일반 F의 보조 손 교체는 유지합니다.

`/hoi menu`로 국가 메뉴를 엽니다. 서버가 제공하는 Polymer 리소스팩을 수락해야 UI 이미지·지도·사단 모델이 표시됩니다. 이미지와 모델은 HOI-resourcepack에서 제공하며 클라이언트 JAR에는 포함하지 않습니다. 공유 `hoi_protocol`은 JAR에 내장되고, 전략 계산·저장은 서버가 담당합니다.

## 빌드와 테스트

먼저 인접한 `HOI-resourcepack` 저장소에서 `python scripts/build_pack.py`를 실행합니다. 이미 변환한 PNG가 포함되어 있어 재빌드에는 Steam이나 Pillow가 필요하지 않습니다. 다른 위치의 팩은 Gradle에 `-PhoiResourcePack=<resources.zip의 절대 경로>`로 지정합니다. 이후 Java 25로 실행합니다.

```bash
./gradlew test runGameTest remapJar --console=plain --no-daemon
```

Windows PowerShell:

```powershell
.\gradlew.bat test runGameTest remapJar --console=plain --no-daemon
```

출력물은 **`build/libs/hoi-client-0.1.0-SNAPSHOT.jar` 하나**입니다. `test`는 프로토콜·레이아웃 JUnit, `runGameTest`는 실제 Minecraft에서 외부 팩 로딩, 10개 메뉴, 패널 비율, 6개 기준 연구 슬롯, 첩보 선택·응답 대기·권한 만료, 키보드 비행과 창 크기 변경을 검사합니다. 개발 실행은 `./gradlew :client:runClient`입니다. Linux에서는 `xvfb-run -a ./gradlew test runGameTest remapJar`를 사용합니다. CI는 `HOI-resourcepack`에 등록한 읽기 전용 배포 키의 개인 키를 `HOI_RESOURCEPACK_SSH_KEY` Actions 저장소 시크릿으로 사용합니다. 해당 키는 이 비공개 리소스팩 저장소에만 접근하며, 체크아웃 후 자격 증명을 유지하지 않습니다. 팩 버전은 워크플로의 커밋 SHA로 고정합니다. 팩을 갱신할 때 해당 SHA도 함께 갱신하고 렌더링을 검증합니다. 이 시크릿을 사용할 수 없는 외부 PR에서는 전체 렌더링 검증을 실행할 수 없습니다.

테스트 이미지는 `client/build/run/clientGameTest/screenshots/`에 생성됩니다. UI 테스트는 시험용 데이터로 렌더링을 확인하며, 실제 다중 접속 서버에서 연구 완료까지의 전체 연동 검사는 별도입니다.

## 화면과 게임 규칙

메뉴는 HOI식 금속 프레임, 어두운 내용 영역, 입체 제목 띠와 회색 선택 표시를 사용합니다. 상단은 원작 버튼 이미지의 테두리를 그대로 살린 왼쪽 도구 모음이며, 화면 전체에 넓은 버튼을 늘어놓지 않습니다. 국가 정보의 하위 탭과 경제·무역 전환도 아이콘만 표시하고 접근성 이름을 유지합니다. 새로고침 버튼과 서버 현황 표시는 없으며 서버 현황 자동 조회는 유지합니다. 하단에 기관 관리처럼 실제 동작이 없는 메뉴는 내용 영역을 끝까지 사용합니다. 이미지 팩은 기존 HOI-resourcepack을 사용하며 추가 이미지를 JAR에 넣지 않습니다. GameTest는 일반·소형 창의 메뉴, 상세 창, 하위 탭 및 버튼 겹침을 실제 렌더러에서 검사합니다. 테스트에 보이는 국가 수치는 화면 검증용 데이터이며 운영 서버의 현황이나 다중 접속 동기화 검증을 뜻하지 않습니다.

상단 메뉴 순서는 국가 정보, 결정, 정보기관, 연구, 무역 & 경제, 건설, 생산, 모병 및 배치, 군수, 장교단입니다. 공용 메뉴는 연구 슬롯·트리·기술 상세에서도 유지합니다. 국가 정보 버튼은 왼쪽에서 두 줄 높이를 차지하고 오른쪽은 높이 약 40%의 지표 줄과 약 60%의 메뉴 아이콘 줄로 구성합니다. 스크린샷처럼 작은 자원 표시 줄 아래에 메뉴 아이콘을 배치하며, 국가명·국가 코드·시간·배속과 업적은 상단에 넣지 않습니다. 지표는 정치력·안정도·전쟁 지지도·인력·공장·연료·병참 상황·수송·지휘력·육군 경험치·공군 경험치·해군 경험치·집권 정당 지지도 순서입니다. 핵폭탄·실질 GDP·국가부채도 표시합니다. 세계 긴장도는 국가 정보 버튼과 같은 크기로 오른쪽 끝에 고정하고 TFR 원본 DEFCON 이미지 아래에 %를 표시합니다. `world_tension_icon_big_strip.dds`의 10개 프레임을 10% 단위로 전환하며 0~9%는 첫 프레임, 90~100%는 마지막 프레임입니다. 숫자와 DEFCON 글자, 색상, 투명도를 그대로 보존하며 같은 숫자가 두 구간에 반복되는 원본 배열을 유지합니다. 서버 값이 없으면 특정 DEFCON 단계를 추정하지 않습니다. 핵폭탄은 연구 여부와 관계없이 게임 시작부터 나타납니다. 공장 아래는 에너지, 병참 상황 아래는 보급 효율, 수송 아래는 수송 효율 막대이며 0..1의 서버 값이 있을 때만 초록색으로 채웁니다. 작은 창에서 모든 지표가 들어가지 않으면 상단 줄에 마우스를 올리고 휠로 나머지 지표를 볼 수 있습니다. 모든 화면 크기에서 공용 메뉴 탭의 라벨을 숨기고 작은 원본 버튼 안에 아이콘을 중앙에 표시하며 메뉴 이름은 접근성 이름으로 제공합니다. 연구에서 다른 메뉴로 전환하면 연구 세션을 닫고 서버에서 해당 국가 현황을 새로 조회합니다. 패널 폭은 첨부된 2560px 화면과 TFR 원본 `interface/country*view.gui`의 컨테이너 폭을 기준으로 합니다. 국가 정보 726px(약 **28.4%**), 무역·경제 620px(약 **24.2%**), 군수 560px(약 **21.9%**), 연구·정보기관·모병·건설·생산·결정·장교단은 550px(약 **21.5%**) 비율로 표시합니다. 작은 창이나 큰 Minecraft GUI 배율에서는 글자와 버튼을 위해 국가 248, 무역 244, 기본 184 GUI 단위의 최소 폭을 보장합니다. GUI 2를 기준으로 하며 다른 배율에서도 고정 35%로 늘리지 않고 이 원본 비율을 우선합니다. 읽을 공간이 부족할 때 최소 폭까지 확장하며, 일반 화면에서는 정치 40%·기타 35% 안에 둡니다. 최소 폭도 들어가지 않는 작은 창은 화면 안에서 읽을 수 있는 폭을 우선합니다. 원본 파일은 개발 때 치수만 참조하며 실행 시 Steam 설치를 요구하지 않습니다. 상세 창과 전체 연구 트리는 별도로 열립니다. 사이드바에서 설정된 이동·점프·내려가기·달리기 키를 사용할 수 있고, 상세 창·검색·전체 트리·포커스 이탈 시 이동 입력을 해제합니다.

연구 슬롯 배치는 최대 6개까지 수용하며 실제로는 서버가 제공한 슬롯만 표시합니다. 현재 국가의 최대 슬롯 수는 5개이고 클라이언트에서 빈 슬롯을 추가하지 않습니다. 페이지 번호와 좌우 이동 버튼은 표시하지 않습니다. 슬롯 원본 이미지는 배경으로 유지하고 진행 중인 기술 이미지를 슬롯 중앙에 겹쳐 표시합니다. 슬롯에는 순번을 붙이지 않고 연구 선택 라벨을 배경 안쪽으로 내려 배치하며 저장 일수 문구는 슬롯에서 숨기되 서버의 저장 일수와 남은 연구 기간 계산은 유지합니다. 상단 연구 배너는 원본의 가로 비율로 제한하고, 작은 창에서는 6개 슬롯이 들어가도록 높이를 더 줄입니다. 내부 8개 연구 분야를 유지하며 화면에는 보병, 지상 & 항공 지원, 기갑, 포, 해군, 해군 지원 장비, 공군, 공학, 산업의 9개 아이콘 탭을 표시합니다. 탭에는 라벨을 붙이지 않고 접근성 이름만 제공합니다. `category/`와 `tabs/`는 같은 원작 UI 아이콘을 사용하며, 선택한 탭의 아래 테두리는 연구 패널에 이어집니다. 기술 상세에도 해당 아이콘 탭을 부착합니다. 모든 이미지는 원본 비율을 유지합니다. 상단 연구 배너만 영역을 채우도록 중앙 부분을 잘라 표시하고 나머지는 지정 영역 안에 중앙 정렬하며, 탭·사진의 최대 크기는 패널 너비와 독립적으로 제한합니다.

휠로 세로 이동, 가로 휠·우클릭 드래그로 트리를 이동합니다. 기술 상세에서 이미지·연구일·효과·해금 장비를 확인하고 실제 시작·중단을 요청합니다. TFR 전체 DLC 연구 415개를 원본 좌표와 선행 연결로 표시합니다. 보병·지원·해군·해군 지원은 가로형, 기갑·포병·공군·공학·산업은 세로형입니다. 원본 기술은 작은 카드로 표시하고 사진·이름을 유지하며 전체 이름은 상세 창에서 확인합니다. 겹치는 원본 카드는 필요한 부분만 이동합니다. 기존 예제 기술은 진행·완료 이력이 있을 때 함께 표시합니다.

정보기관은 서버가 제공한 고용·해고, 임무 및 대상 선택, 23개 업그레이드, 해독 시작·정지·공개, 3개 작전 준비·취소를 지원합니다. 비용·조건·소유권·진행·저장은 서버에서 처리하고 클라이언트는 일회용 세션 리비전으로 요청합니다. 국가 중점 목록은 현재 이벤트 플래그·분기·선행 조건을 재조회합니다. 지휘관 상세는 실제 서버 능력치와 배속 군을 표시합니다.

게임 시작일은 **2020-01-01**이며 X1은 **10틱/시간**, 속도는 일시정지·X1~X5입니다. 보병 연도 축은 **2000년부터**, 기갑은 **1980·2018·2020·2022·2024·2028·2032년**입니다. 원본 415개 외에 기존 예제 기술 8개의 저장은 유지합니다. 국가 효과 29종(215항목)과 9개 기술의 시설 해금은 실제 서버 계산에 연결됩니다. 원본 장비·모듈·부대 해금, 장비별 능력치, 일부 경제 및 국가별 완료 스크립트는 상세창의 적용 대기 항목이며 실행하지 않습니다. 정치 및 거시경제 수치는 읽기 전용 현황이며 전체 법률 교체 UI, 원작 후보 요원·무작위 작전 단계, 육해공군 잠입·거짓 정보 작전, 지휘관 특성 트리·임명, 3D 미리보기는 아직 이식되지 않았습니다. 첩보는 HOI 코어에 구현된 세 작전과 실제 기간·조건을 사용합니다. 실제 다중 접속 인증·진행·팩 다운로드 검증은 별도입니다.

연구 개요에는 슬롯 개수 라벨을 표시하지 않습니다. 건설 속도와 사용 가능한 에너지는 원본 아이콘 옆에 숫자만 표시하고, 건설 속도의 정수 증감률과 에너지 값이 0 이상이면 초록색(`#55FF55`), 음수이면 dark_red(`#AA0000`)를 사용합니다. 항목명과 서버에서 받은 계산 내역은 호버 툴팁에서 확인합니다. 계산 내역이 없는 구버전 스냅샷에는 제공되지 않았다고 표시하며 클라이언트에서 계산을 추정하지 않습니다.

건설 상단 민간공장과 수리 아이콘은 HOI4 바닐라 `non_available_factory_icon.dds`, `factory_wrench_icon.dds`를 외부 팩에서 읽습니다. 민간공장 배정량/총량은 모두 배정했으면 초록색(`#55FF55`), 남은 공장이 있으면 gold(`#FFAA00`)입니다. `미사용 n` 요약 라벨은 숨기되 공장 배정 내역 툴팁과 서버 수치는 유지합니다.

연구 상세를 제외한 패널은 요청 성공·실패·시간 초과 메시지를 내부나 하단에 표시하지 않습니다. 서버 검증, 응답 대기 중 중복 요청 방지와 자동 재조회는 유지합니다.

건설 상단 에너지 숫자는 서버 공급량에서 수요를 뺀 잔여량입니다. 음수이면 TFR `missing_resources_strip.dds`의 에너지 프레임, 0 이상이면 `resources_strip.dds`의 에너지 프레임을 표시합니다. 상단 요약의 테두리는 지표 아이콘 아래 4 GUI 단위의 내부 여백과 소비재 행 앞 3 GUI 단위의 간격을 둡니다. GUI 너비가 800 이상이면 패널 너비에 원본 비율을 직접 적용하고, 더 좁은 창에서는 기존 가독성 최소 너비를 유지합니다.

## 소스와 프로토콜

| 경로 | 내용 |
|---|---|
| [client](client/src/main/java/dev/hoi/client) | 연구 UI·입력·클라이언트 네트워크 |
| [protocol](protocol/src/main/java/dev/hoi/protocol) | 공유 연구·산업·건설 패킷과 비공개 화면 데이터 |
| [클라이언트 테스트](client/src/gametest) | 실제 Minecraft 렌더러 테스트 |

기존 [HOI의 `2567c2f`](https://github.com/Kyarurung/HOI/commit/2567c2fb7b439835e64d1220690be22dbc30792d)에서 분리했습니다. 원래 개발 이력은 HOI에 남아 있습니다. `protocol/`은 [HOI의 같은 모듈](https://github.com/Kyarurung/HOI/tree/main/protocol)과 동일한 소스로 시작합니다. 공유 모듈을 수정할 때는 두 저장소에 함께 반영하고 호환되지 않는 변경은 패킷 버전도 올립니다. 자동으로 다른 저장소의 소스를 가져오지는 않습니다.

수신기 등록 전 `ResearchProtocol.registerPayloadTypes()`를 호출해야 합니다. 독립 모드 초기화 순서에 의존하면 서버 시작이 실패합니다. 게임 수치·비용·완료 여부는 서버만 결정합니다.

## 서버 명령과 시간 표시

클라이언트는 /hoi 루트를 등록하지 않습니다. 이전 버전의 client-sided command 오류는 클라이언트 루트가 서버의 start·map·ready·stop을 가로챈 것이 원인입니다. 최신 클라이언트 JAR로 교체하고 클라이언트를 재실행하세요. /hoi research는 서버가 화면 열기를 요청하고 클라이언트가 기존 인증·요청 제한을 적용하는 OPEN 요청을 보냅니다. /hoi-research는 독립적인 명령이며 Shift+F는 국가 메뉴를 엽니다.

날짜·시간은 2020년 1월 1일 01시, 배속은 1배속~5배속으로 표시합니다. 배속 색상은 초록·노랑·주황·빨강·진한 빨강이며 | 구분선은 진한 회색입니다. 배속과 일시정지는 관리자만 변경합니다. 관리자 /hoi stop은 국가 선택과 캠페인을 초기화하고 2020년 1월 1일 00시·일시정지로 되돌립니다. 초기화 백업은 서버에서 보관하며 Minecraft 서버 자체는 종료하지 않습니다.

클라이언트 GameTest는 실제 등록 함수를 호출하여 /hoi를 등록하지 않고 /hoi-research만 등록하는지 확인하며, 연구 헤더의 한국어 날짜·배속을 실제 렌더러로 검사합니다. 운영 서버와 플레이어 클라이언트의 재접속 검증은 별도입니다.

상단 수치는 메뉴·연구의 기존 비공개 JSON 스냅샷에 선택적 `hud`·`national` 필드로 전달합니다. 구버전 서버의 누락 필드는 알 수 없음(`—`)으로 처리하고 기존 wire ID를 유지합니다. 정치력·유효 안정도·전쟁 지지도·인력(예비 인력)·공장(민간/군수/조선소 합계)·연료·수송선·지휘력·군종 경험치·집권 정당 지지도·세계 긴장도는 실제 서버 값입니다. 실질 GDP·부채는 저장된 국가 회계 값(십억 달러)을 읽으며 실행 중인 TFR 이야기 변수 `gdp_total_var`·`debt_var`가 있으면 이를 우선합니다. 사용자가 지정한 한국의 새 캠페인 시작값은 실질 GDP **1.707조(1707십억)**, 부채 **704.314십억**입니다. 클라이언트는 GDP와 부채를 모두 조 단위로 소수점 셋째 자리까지 표시하며 툴팁에 전체 원값을 표시합니다. 이후 변경값은 저장·불러오기와 국가 갱신에서 보존합니다. 기존 국가 저장에 회계 필드가 없으면 `—`로 유지하고 시작값으로 덮어쓰지 않습니다. 현재 기본 캠페인에는 국가 단위 병참량·보급/수송 효율과 전체 TFR 경제·핵무기 체계가 연결되지 않아 해당 미등록 값은 `—`, 효율 막대는 빈 상태입니다. 핵폭탄 항목은 시작부터 표시하며, 실제 등록된 핵무기 기술 `nukes`에 연결된 장비 재고를 연구 완료 여부와 관계없이 집계합니다. 재고 종류가 등록되었으면 0개도 표시하고 종류 자체가 없으면 `—`입니다. UI를 열어 수치·기술·무기를 생성하지 않습니다. UI GameTest의 채워진 막대와 수치는 화면 검증용이며 실제 다중 접속의 동기화를 증명하지 않습니다. 새 HUD 아이콘 팩과 HOI 서버 개발 빌드가 함께 필요하며 운영 서버는 자동으로 시작하거나 재시작하지 않습니다.


정치력은 소수부를 버리고 `-2000`부터 `2000`까지 다섯 글자 정수로 표시합니다. 정치력 칸은 54 GUI 단위, 수송선 칸은 52 GUI 단위로 `10.0K`까지 잘리지 않게 표시합니다. 서버의 정치력 상한 2000을 유지하며 지휘력은 소수부 없이 세 자리 정수까지 잘리지 않게 표시합니다. 툴팁의 원값과 누락 값 `—` 처리는 유지합니다. 건설 대기열 툴팁은 내부 건물 ID 대신 서버가 제공한 이름(민간공장·군수공장 등)을 표시합니다. 숫자 경계값은 JUnit으로, HUD 글자 폭과 번역된 건설 화면은 실제 클라이언트 렌더링으로 확인하며 운영 서버 접속 검증은 별도입니다.

건설 선택 목록은 주 건물(기반시설·공군기지·대공포·레이더), 공유 건물(공장·조선소·정유시설 등), 지역 건물(보급허브·해군기지·요새 등) 순서로 배치합니다. 별도 분류 제목이나 탭 없이 두 줄의 구분선만 사용하며, 작은 창에서는 전체 목록을 스크롤합니다. 댐·산악 댐은 선택 목록에서 제외하며 기존 지도 시설이나 서버 저장값은 변경하지 않습니다.

2026-09-09 건설 변경 검증: JUnit과 실제 UI 렌더링에서 구분선 간격·댐 제외·소형 창 스크롤·좌클릭 건설/우클릭 취소 요청을 확인했습니다. 오키나와 해협 경로 검사를 포함한 전체 GameTest도 통과했으며, 운영 서버에서의 실제 접속 검증은 별도입니다.

## TFR 연구·혜택 검증 (2026-09-08)

연구 카테고리는 원본 이미지의 투명한 좌우 여백을 제외해 거의 붙여 표시합니다. 연구 카드는 기술명 띠 없이 비율을 유지한 이미지만 표시하고 이름은 툴팁과 상세창에 남깁니다. 장비 구분 제목은 반드시 해당 연구 대상 위에 배치하며, 실제 글꼴 크기로 카드·다른 제목과의 겹침을 피합니다. 연도 표시에도 여유를 확보하고 모든 연구 트리의 위아래에 빈 한 줄을 둡니다. 상세창 위의 카테고리 돌출 탭은 제거했습니다. 일반·소형 창의 전체 카테고리를 실제 렌더러로 검사하며 서버의 연구 판정과 진행량은 바꾸지 않습니다.

배너 아래에 제한된 혜택의 남은 횟수와 국가 연구 속도를 표시합니다. 혜택 버튼은 종류·대상·속도·선행 연도 감소를 안내합니다. 선택한 슬롯이 사용 중이면 남은 빈 슬롯을 자동 선택하고 서버도 이를 검증합니다. 선행 AND/OR, 배타 분기, 국가별 중점·플래그 조건과 예상 일수를 서버가 결정합니다. 슬롯을 중단·재개해도 혜택을 다시 소비하지 않습니다.

연구 payload는 v2이며 서버와 클라이언트의 공유 protocol 소스를 함께 갱신해야 합니다. v1 연구 패킷과 호환되지 않습니다. 상한은 512개 기술, 16개 슬롯, JSON 400,000문자입니다. 국가 메뉴·첩보 payload ID는 유지됩니다. 공통 연구 사진 413개와 대한민국 전용 항공기 사진 2개는 HOI-resourcepack에서 읽습니다. 12인치 부포는 연구 목록에서 제거했습니다. 두 항공기의 공통 원본이 없는 다른 국가는 기존 분야 아이콘을 유지합니다.

`test runGameTest remapJar`는 415개 시험 스냅샷으로 모든 탭의 배치·클릭, 가로·세로 방향, 슬롯 자동 선택, 혜택·상세창과 작은 창을 확인합니다. 실제 Minecraft 렌더러의 `hoi-tfr-*` 스크린샷을 `client/build/run/clientGameTest/screenshots/`에 기록합니다. 시험 값 +4%·혜택 2회는 국가에 지급하지 않습니다. 전체 fixture는 배포 JAR에 포함하지 않습니다. 실제 운영 서버 연결에서 연구 시작~완료, 지연·재접속, 리소스팩 다운로드 검증은 남아 있습니다. 운영 서버를 자동 변경하거나 시작하지 않습니다.

기갑 33개와 산업 60개는 2026-09-08 추가 스크린샷에 맞춘 전용 배치입니다. 기갑은 엔진·정찰전차·주력전차·수륙양용·장갑·보조 무기/탄약의 열을 분리하고, 차체만 넓은 카드로 표시합니다. 산업은 작은 정사각형 아이콘으로 생산·산업·건설/채굴·합성 석유를 구분합니다. 같은 행의 연구는 좌우로 연결하며 다른 행은 공통 분기선으로 연결합니다. 기존 원본 좌표에 겹침이 있어도 해당 열을 유지합니다. 양쪽 연도 축은 화면 참조이고 실제 기술 연도·효과·선행 조건은 서버 정보를 그대로 사용합니다. 큰 화면에서는 비율에 맞춰 확대하고 작은 화면은 스크롤로 접근합니다. 415개 기술 전체에 9개 탭별 고정 배치를 적용합니다. 연구 탭은 좌측에 나란히 놓고 본문 상단에 붙이며 원본 선택·비선택 프레임을 전환합니다. 기술 갈래는 원본 XOR 이미지로, 특별 묶음은 테두리로 표시합니다. 기술 검색과 처음 위치/슬롯 목록 버튼은 없으며 Esc로 돌아갑니다. `hoi-armor-rearranged`, `hoi-industry-rearranged`와 소형 창 스크린샷으로 검증합니다.


## 건설 예약과 공장 배정

건설 메뉴는 원본 비율의 왼쪽 패널과 원본 건물 아이콘을 사용합니다. 건물을 선택하면 건설 지도 모드로 전환합니다. 이후 1인칭 시점에서 지도 위의 자국 주를 좌클릭하면 서버가 소유권·현재 통제·연구·슬롯을 검사하고 건설을 예약합니다. 우클릭은 해당 주에서 선택한 건물의 마지막 건설 예약 하나를 취소합니다. UI를 닫고 건설 지도 도구를 든 상태에서도 같은 조작을 사용합니다. 완성된 건물은 철거하지 않습니다. 목록의 우선순위 변경·취소와 수리 우선 공장 −/+는 서버에 요청하고 저장합니다. 수리 우선 공장은 제목과 분리된 숫자 칸을 사용합니다. 작은 창의 건설 목록과 건물 아이콘 목록은 각각 스크롤합니다.

소비재·무역/기관 예약을 먼저 제외하고, 손상 건물이 있을 때 수리 우선 공장을 확보한 뒤 각 건설에 최대 15개를 배정합니다. 남는 공장 중 최대 15개는 수리에 사용하며 나머지는 미사용으로 표시합니다. 같은 날 건설을 마친 공장을 수리에 중복 배정하지 않습니다. 소비재, 사용/총 공장, 미사용 공장, 건설 속도, 에너지 공급/수요 및 실제 수리 배정은 같은 서버 계산을 사용합니다. 에너지 충족률은 상단 공장 아래 막대에도 반영합니다. 수리 설정은 기존 국가 저장에 선택적으로 추가되어 이전 저장은 0개 우선 지정으로 시작하며 진행을 초기화하지 않습니다.

현재 건설 가능한 종류는 등록된 민간공장·군수공장·조선소·기반시설·공군기지·대공포·레이더·정제시설·연료 저장고 9종입니다. 조선소는 항구가 있는 주에만 예약합니다. 나머지 원본 아이콘은 조건 안내와 함께 비활성화하며, 미등록 발전소·댐·원자로·지역 시설을 임의 생성하지 않습니다. 에너지는 현재 등록된 석탄 자원과 무역 결과를 이용합니다. 공급은 `10 + 9 × 석탄`, 수요는 `공장 수 × min(6.6, 0.25 + 0.0125 × 공장 수)`인 현재 HOI 규칙입니다. 에너지 충족률에 따라 건설 공장당 작업량은 4~5, 군수공장 생산은 3.5~4.5, 조선소 생산은 2~2.5로 변합니다. 발전소 생산·특수 프로젝트 수요·TFR의 모든 에너지 보정과 전력 거래는 아직 구현하지 않았으며 원작 전체 전력망과 동일한 계산으로 간주하지 않습니다.

건설 요청/응답은 공유 `hoi:construction_request_v2`·`hoi:construction_response_v2`을 사용합니다. 서버가 발급한 세션과 단일 사용 리비전으로 중복 예약을 차단하고, 국가 권한 상실 시 화면을 닫습니다. 지도 요청에는 국가나 주 ID 대신 제한된 시선 방향을 보내고 서버가 플레이어 위치에서 주를 찾습니다. 기존 연구 payload는 변경하지 않습니다.

검증에는 공장 배정·에너지 부족·저장 복구 JUnit, 실제 지도 좌표와 일반 지도 클릭의 서버 GameTest, 작은 창의 수리 버튼·목록 스크롤·권한 만료를 포함한 실제 클라이언트 렌더링이 포함됩니다. UI fixture와 mock 서버 플레이어 검증이며 실제 다중 접속의 클라이언트→서버 왕복·재접속·팩 다운로드 검증은 별도입니다. 운영 서버는 시작하거나 재시작하지 않습니다.

Politics uses seven icon rows (government, economic laws, military laws, social laws, development, military staff, research & production) and widens to a readable minimum of 248 GUI pixels. Foreign-country inspection has diplomacy and civilian/army/navy/air ledger tabs; unknown fields stay unknown. The server filters every field at its own intelligence threshold before sending it. Allies share implemented information without changing their displayed espionage percentage. Target changes rotate correlation tokens and clear the preceding private screen. Actual renderer fixtures cover normal/compact views and stale/revoked responses, not multiplayer end-to-end synchronization. Historical graphs, raid/decoy detection and route overlays require additional server reporting data and are not simulated by this client.


## TFR 강·지형 모델 렌더링 검사

`AtlasSurfaceRenderChecks`는 서버가 내보낸 한국 주변 실제 국가색·전체 블록 운반 상태와 정적 도형을 일회용 월드에서 확인합니다. 새 `hoi:atlas_scene_v2`은 경계·강·묘목·진흙·모래·도시를 디스플레이 엔티티 없이 32블록 타일·재질별로 렌더링합니다. 도형 페이지가 모두 조립되면 서버에 확인을 보내 기존 표시를 해제합니다. 불완전·혼합 장면은 게시하지 않으며 새 페이지 수신 전에는 서버의 호환 표시가 유지됩니다. 도시 모델은 외부 팩을 사용하고, 육군 강은 vanilla 애니메이션 물 텍스처, 해군·공군 강은 각 바다의 기본 콘크리트 재질을 사용하며 검정 장식 테두리는 표시하지 않습니다. 좁은 물을 경로 중앙에 맞춰 기존 경계선 위에 불투명하게 표시하므로 물 밖으로 검정 선이 비치지 않습니다. 국가·주·프로빈스 경계 도형은 삭제하지 않습니다. 언덕 1칸·산악 2칸 실제 지형과 소형 지형은 모든 지도에 표시하고, 강 구간은 육군·해군·공군에 표시합니다. 주·프로빈스·강은 높은 쪽 지형과 ㄱ자 높이 연결을 따릅니다. 해군은 육지 주·해역·해상 프로빈스, 공군은 제공권·육지/바다 이음선을 표시합니다. 새 서버의 해안 ID 보정과 동일한 좌표로 사진의 빈 사각형 위치를 검사합니다. 경계는 서버가 실제 셀 마스크로 잘라 보낸 면을 사용하며 화면 밖 교차 구간도 시험 장면 범위로 자릅니다. 실제 렌더러는 물 높이·도시 확대·수도 이름·승점도 촬영하지만 운영 서버의 payload 왕복·재접속·다중 접속·FPS/MSPT 검증은 별도입니다. 새 서버와 클라이언트 JAR를 함께 사용해야 하며 정적 에셋과 이 fixture는 배포 JAR에 포함하지 않습니다.

표시 숫자는 최대 소수점 두 자리에서 반올림하며 저장·계산 정밀도는 서버에서 유지합니다. 수도 이름 크기는 서버의 디스플레이 설정을 따릅니다.

지도 도형은 16블록 타일로 나누고 카메라의 높이 차이를 포함한 거리로 숨깁니다. 경계와 강·묘목·진흙·모래·도시 모델은 모두 64블록 범위이며 가까워지면 다시 표시합니다. 시야 밖 타일도 제출하지 않습니다. 실제 지형 블록의 청크 렌더링 거리는 이 설정과 별개입니다. 실제 렌더러 GameTest는 가까움→64블록 안→64블록 밖→복귀에서 8종 재질 타일이 8→8→0→8개로 바뀌는 것을 검사합니다. 16블록 공간 인덱스에서 가까운 후보만 검사하며, 타일 하나가 64개보다 많은 버킷을 차지하면 별도 거리 검사로 메모리 증가를 제한합니다. 멀리 배치한 256개 도형의 검색 제외와 1,024블록 긴 도형의 표시도 검사합니다. 이는 표시 제한 검증이며 실제 사용자 클라이언트의 FPS 상승 수치는 측정하지 않았습니다.

## 생산·무역·군수·모병 연결

생산, 무역 & 경제, 군수, 모병 및 배치 메뉴가 공유 `hoi:industry_request_v2`·`hoi:industry_response_v2`로 연결됩니다. UUID 국가 권한, 서버가 발급한 선택지와 단일 사용 리비전을 검증하며 정상 변경은 서버 저장을 요청합니다. 구버전 서버에는 이 조작 프로토콜이 없어 기존 조회만 사용할 수 있습니다.

- 생산: 보병 및 포병 장비 제작·기갑 차량 제작·항공기 제작·함선 건조 버튼으로 등록·해금된 장비를 선택합니다. 라인 교체·삭제·우선순위와 공장 −/+를 조작합니다. 군수공장의 라인당 150개 제한을 없앴으며 실제 국가 보유 공장 수가 상한입니다. 함종별 조선소 5/10/15개 제한은 유지합니다. 일일 생산량과 7종 자원 수요·생산 효율은 실제 서버 계산을 표시합니다. 안정도 생산 효과·전력 충족률 항목은 화면과 툴팁에서 생략합니다. 설계사/MIO는 포함하지 않습니다.
- 무역: 자원별 추출·수입·수출 배정·생산 수요·잔여량을 보고 가용 민간공장으로 수입 계약을 체결하거나 취소합니다. 소유한 섬 하나 때문에 육상 경로가 없다고 판정하지 않으며 모든 자국 통제 지역에서 연결을 확인합니다. 경제 화면은 저장된 실질 GDP·부채, 공장 배정, 전력·연료·인력 현황을 표시합니다.
- 군수: 연구로 해금됐거나 창고 비축량이 있는 장비만 표시합니다. 서버가 지정한 같은 장비 계열은 수량과 일일 생산량을 합산하고 최신 연구 완료 모델의 이름과 이미지를 사용합니다. 구형 장비 재고를 신형으로 변환하지 않으며 마우스를 올리면 모델별 창고·훈련 지급·배치 사단 휴대·부족 수량을 볼 수 있습니다. 미연구 장비도 비축량이 있으면 포함하지만 최신 연구 이미지 선택에서는 제외합니다. 지도 자원은 매일 공급 가능한 생산 자원량이며 광석을 별도 창고에 쌓는 방식은 아닙니다. 자원 부족은 기존 서버 생산 페널티를 적용합니다.
- 모병: 자국 배치 장소와 편제를 선택해 훈련을 예약하고 우선순위를 변경하거나 취소·조기 배치합니다. 실제 인력과 창고 장비가 지급되고, 보급 경로·자국 통제·장비 충족 조건 아래 시간이 진행됩니다. 배치 장소가 지정되면 훈련 100%에 자동 배치하며 조기 배치는 20% 이상에서 서버가 검사합니다. 취소 시 지급 인력·장비를 반환합니다.
- 사단 설계: 대대는 5열×5행이며 각 열의 첫 칸과 배치된 대대 바로 아래 한 칸을 독립적으로 엽니다. 일반 지원중대는 위에서부터 한 칸씩 열리며 이미 넣은 중대는 선택 목록에서 제외합니다. 연대 지원은 해당 열에 대대가 3개 이상일 때 +가 나타나는 장식이며 선택·능력치·비용에 포함하지 않습니다. 이전 연대 지원 저장 필드는 읽을 때 비웁니다. 상단에는 기갑 비율·조직력·내구도·속도를, 오른쪽에는 기본 능력치·전투 능력치·장비 비용을 원본 아이콘과 함께 표시합니다. 예상 생산 비용은 서버가 발급한 장비 수량×단가의 합계이며 알려지지 않은 단가는 추정하지 않습니다. 새 편제로 저장해 기존 사단·훈련을 바꾸지 않으며 작은 창은 스크롤합니다.

`IndustryView.Template.columns`는 5개 열의 대대 수입니다. `regimentSupport`는 구버전 저장 호환용이며 새 편제에서는 비어 있습니다. 열 편집은 `LINE_COLUMN`을 사용하고 `REGIMENT_SUPPORT` 요청은 거부합니다. 생산 순서의 `UP`·`DOWN`은 기존 enum 뒤에 추가했으며 기존 action 순서와 payload ID를 유지합니다. 클라이언트와 서버의 protocol 모듈을 함께 빌드해야 합니다. 서버 테스트는 열별 편집·요청 검증·저장을, 클라이언트 GameTest는 서버가 생성한 fixture의 실제 렌더링을 확인합니다. 실제 운영 서버 접속 검증과는 구분합니다.

현재 콘텐츠의 대대 4종·지원중대 5종과 등록된 생산 장비를 사용하며 스크린샷의 미등록 부대·전투 수치는 추가하지 않습니다. 명목 GDP·인플레이션·대출/상환·양적 완화/긴축 등 원작 전체 거시경제 정책, 원작 OOB 자동 설치와 3D 병사 미리보기는 미구현입니다. 국가별 인구와 기존 징병 법률에 따라 서버 시간이 흐르며 인력이 동원되며, 메뉴를 여는 것으로 인력이나 장비를 지급하지 않습니다.

서버 검증은 점유 자원 생산→창고 재고→훈련 지급→장비를 휴대한 사단 배치와 중간 저장 복구, 무역 공장·자원 이전/취소, 중복·잘못된 요청·권한 변경을 검사합니다. 클라이언트 GameTest는 서버에서 캡처한 fixture로 실제 Minecraft 렌더링·버튼 요청·일반/소형 창·툴팁과 `11.6K` 표시를 확인합니다. 스크린샷은 `client/build/run/clientGameTest/screenshots/`의 `hoi-industry-*`, `hoi-construction-*-tooltip`입니다. 실제 다중 접속 왕복·팩 다운로드·운영 서버 재접속 검증은 별도입니다.

해군 수리 대기열은 국가 소유 손상 함선의 내구도·귀항 상태·항구와 건조에 쓰이지 않는 조선소 수를 서버에서 조회합니다. 실제 수리는 기존 서버의 일일 처리·접안 권한·항구 수용량을 따르며 이 창은 수리 조작이나 즉시 회복을 수행하지 않습니다. 선택적 `navalRepairs` 필드는 기존 v1 JSON과 호환됩니다. 구버전 서버에서 보고가 없으면 빈 대기열로 간주하지 않고 확인 불가로 표시합니다. 렌더링용 손상 함선 예제는 실제 서버 함선이 아닙니다.

자원·장비·대대 이미지, 사단 설계의 원본 `+`·잠금 그림, 생산 전용 5개 버튼과 군수공장·조선소 아이콘은 외부 팩의 `production/`에서 읽습니다. 패키지에 이 경로가 없는 구형 팩으로 접속하면 누락되므로 클라이언트 JAR뿐 아니라 **운영 서버의 원본 팩과 Polymer가 제공하는 팩도 함께 갱신**해야 합니다. `HOI-resourcepack/scripts/build_pack.py`로 빌드한 뒤 운영 서버가 종료된 상태에서 `HOI-server/scripts/install-resourcepack.py`를 사용합니다. 다음 명시적 서버 시작 때 Polymer가 이를 포함합니다. 시작·재시작은 자동 수행하지 않습니다. 건설의 수리 우선 칸은 제목에서 5 GUI 픽셀 더 아래에 표시하며 작은 창에서도 건설 목록 공간을 유지합니다.

군수공장·조선소·수리 조선소의 생산 요약 아이콘은 HOI4 바닐라 원본입니다. 생산 보정 6종은 조선소 생산량·공장 생산량·효율 한도·효율 유지·효율 성장·공장 폭격 취약도 순서이며 별도 원본 아이콘을 사용합니다. 안정도 생산 효과·전력 충족률은 생산 화면과 툴팁에서 숨기고 기존 서버 계산은 유지합니다.

생산 중인 장비 이미지를 클릭하면 교체 선택 창이 열립니다. 기본 목록에는 연구가 완료된 동일 장비 계열의 최신형만 표시합니다. **구형 장비 표시** 체크란은 이 장비 선택 창에만 있으며 구형 모델을 다시 표시할 수 있습니다. 현재 생산 모델은 표시될 때 `생산 중`으로 비활성화하고 구형 생산 라인·창고 장비에는 `[구형]`을 붙입니다. 별도 신형 교체 버튼은 없습니다. 신형 여부는 서버의 등록 계열·종류·세대와 실제 연구 해금으로 결정하며 이름이나 이미지로 추측하지 않습니다. 교체는 기존 생산 라인의 공장 배정을 유지하고 서버의 효율 손실을 적용하며 창고의 구형 장비를 삭제하거나 변환하지 않습니다. 선택적 `replacement`·`family` 필드가 없는 구버전 응답의 세대는 추측하지 않습니다. 서버 GameTest는 실제 연구 완료·교체·재고 보존·중복 요청 거절·저장 복구를, 클라이언트는 그 응답을 캡처한 fixture로 필터·체크란·교체 요청과 일반/소형 창을 검사합니다.


## 연구 상세·생산 표시 보완 (2026-09-09)

연구 상세창은 실제 연구 효과를 한국어로 표시하고, 외부 팩의 `ui/research_details.json`에서 원본 장비·부품 이미지와 참고 능력치를 읽습니다. 183개 기술의 310개 카드 중 기본 이미지가 있는 카드는 288개이며 기술 그림을 대신 쓰면 `대표 이미지`로 표시합니다. 차체와 엔진 등 부품 수치는 각각 표시합니다. 참고 카드는 실제 장비 지급이나 미지원 효과 적용을 뜻하지 않습니다. 팩이 없거나 유효하지 않으면 해당 카드만 생략합니다. 3D 설계 미리보기는 없습니다.

상세창의 슬롯 전환 버튼·선행 연구 목록·내부 ID는 숨기며 실제 배타 선택에만 택일 설명을 표시합니다. 해금 장비와 시설은 이름과 상세 카드로 표시합니다. 슬롯 목록의 선택과 서버 검증은 유지합니다. `PRODUCTION_BASE`, `DOCKYARD_OUTPUT` 등 효과 키는 한국어로 표시하며 군수공장·조선소 유지비 변동치는 백분율로 표시합니다.

국가 정보 로딩 중에는 빈 회색 국기를 쓰고 국가를 받은 뒤 해당 국기를 표시합니다. 서버의 KOR 표시명은 대한민국입니다. 생산은 자원 7개 → 버프 3열×2줄 → 군수공장·건조 조선소·수리용 조선소 사용/전체 → 제작·수리 버튼 5개 순서로 표시합니다. 버프 첫 줄은 조선소 생산량·공장 생산량·생산 효율 한도, 둘째 줄은 생산 효율 유지·생산 효율 성장·공장 폭격 취약도입니다. 기존 생산 라인의 그림 칸·이름·일일 생산량·공장 조절 버튼·효율 막대 배치를 유지하고 원본 장비 이미지를 넣습니다. 표준 창에는 세 라인이 보이며 작은 창에서는 본문을 스크롤합니다. 수리용 조선소는 서버의 다음 일일 수리 배정 계획을 표시하며 부분 가동한 마지막 조선소도 한 칸으로 셉니다. 구버전 서버의 누락된 사용량은 0 대신 —로 표시합니다.

실제 렌더러 fixture는 세 생산 라인, 함선 차체·엔진/포/산업/공병 상세창, 긴 상세창 스크롤, 작은 창과 빈 로딩 국기를 촬영합니다. 강의 물과 높이 연결도 서버가 내보낸 수정 도형으로 촬영합니다. 운영 서버의 팩 다운로드·양쪽 JAR 교체·재접속·다중 접속 확인은 수행하지 않았습니다.

## TFRKP 알림과 HUD 상세

국가·글로벌·슈퍼 이벤트 창은 아래 GUI 2 참고 치수를 사용하며 작은 창에서는 읽기 가능한 최소 크기를 유지합니다. 국가·글로벌 이벤트의 제목·본문·선택지 글씨는 기본 크기의 90%이며 슈퍼 이벤트 글씨는 기존 크기입니다. Minecraft 기본 폰트를 사용하고 별도 폰트 팩이 있으면 그 설정을 따릅니다. 긴 본문은 스크롤하고 많은 선택지는 기존 선택지 이동 버튼으로 확인합니다.

국가 중점 완료·연구 완료는 화면 중앙의 작은 금속 팝업에 그림과 이름을 표시하며 설명 본문은 넣지 않습니다. 서버는 매 틱 완료 변화를 확인하고 지도 정보창에 가로막히지 않게 알립니다. `세부 사항`은 서버가 세션·리비전·국가 권한을 확인한 뒤 완료한 연구의 트리 위치와 장비 상세 또는 국가 메뉴의 해당 중점 항목을 엽니다. 현재 중점 UI는 목록 기반이며 별도의 원작 중점 트리는 구현되어 있지 않습니다. 선택이 필요한 이벤트와 먼저 도착한 완료 알림은 순서대로 확인합니다. 국가 이벤트·글로벌 뉴스는 본문·원본 그림·서버 선택지를, 슈퍼 이벤트는 큰 원본 그림·인용문·등록된 음원을 표시합니다. 이미지·한국어 문구·음원은 TFRKP를 우선하고 없는 항목만 TFR·바닐라 원본으로 보완합니다. 별도로 지정한 생산 요약의 군수공장·조선소·수리 조선소 아이콘은 바닐라 원본을 유지합니다. 모든 이미지·음원은 외부 팩에서 읽으며 JAR에 포함하지 않습니다. 현재 실행 가능한 중점 12개 중 원본 아이콘 10개를 가져왔고, 찾지 못한 2개는 공통 중점 아이콘으로 표시합니다.

연구 툴팁은 연구명, 미충족 선행 기술, 연구 시간, 소수점 두 자리의 선행 연도 경고, 빈 줄, 효과·사용 가능 장비 순서입니다. 충족한 선행 조건과 과거 기술의 선행 경고는 숨깁니다. 효과는 버프를 `+`·초록(`green`), 디버프를 `-`·진한 빨강(`dark_red`)으로 표시합니다. 비용·소비 감소도 버프이므로 화면에서는 `+`를 사용하며 실제 계산·저장값은 바꾸지 않습니다. 부호 없는 수치·장비명·시설 키워드는 금색(`gold`), 항목명·인력·조직력·훈련 시간·병종명은 흰색입니다. 서버의 비율 0.07은 +7%로 표시하고 +7.0%의 불필요한 소수점은 없애되 2.5%와 장비의 절대 능력치 0.5는 보존합니다. 원본 참고 효과의 적용 대기 구분도 유지합니다.

국가 전용 번역이 있으면 연구명·완료 제목·해금 장비/부품명에 우선합니다. 대한민국의 K1 기관단총·K2 돌격소총은 TFRKP 한국어 원문을 사용하고 괄호 안 모델 병기는 표시하지 않습니다. 선택적 `ResearchView.Source.localizedNames`가 없는 구버전 JSON은 공통 이름으로 표시합니다. 완료 이동용 `hoi:completion_details_v1`은 기존 payload를 변경하지 않고 추가했으며 닫힌 팝업 토큰의 이동 응답은 무시합니다. 이 동작에는 함께 빌드한 HOI 서버 모드와 클라이언트가 필요합니다.

`Shift+F`는 국가 메뉴를 엽니다. 기존 R 연구 단축키는 제거했으며 연구는 메뉴의 연구 아이콘이나 `/hoi-research`로 엽니다. 기본 F의 보조 손 교환은 유지합니다. Enter와 숫자패드 Enter는 완료 팝업을 확인하고, 선택지가 하나인 이벤트는 해당 서버 선택지를 실행합니다. 여러 선택지가 있는 이벤트는 키보드로 선택한 버튼을 실행하며, 선택한 버튼이 없으면 효과를 고르지 않고 창만 닫습니다. 완료 알림은 서버가 접속·국가 변경 시 기준 상태를 잡은 뒤 새로 완료된 항목만 순서대로 전달하므로 과거 완료 전체를 재접속 때 재생하지 않습니다. 클라이언트 확인 버튼은 연구 보상·중점 효과를 다시 지급하지 않습니다.

상단 HUD 툴팁에는 서버가 제공하는 현재 값·일일 변화·보정 출처·사용량을 여러 줄로 표시합니다. 긴 설명은 `Shift+휠`로 스크롤합니다. 선택적 JSON `CountryHud.details`가 없는 구버전 서버는 기존 값과 `세부 내역 없음`을 표시하고, 보고되지 않은 수치는 `—`로 남깁니다. GDP·부채·연료·정치력 등의 설명은 저장된 값과 실제 서버 계산을 사용합니다. 안정도 생산 효과와 전력 충족률 별도 설명은 생략합니다. 전체 TFR 거시경제·정치 시뮬레이션을 이 UI가 새로 구현하는 것은 아닙니다. `DialogView.presentation`도 선택적 필드로 이전 JSON과 호환됩니다.

검증은 실제 연구·중점 완료 감지와 중복/재접속 기준, HUD 조회의 상태 불변 및 일일 연료 계산 일치, 프로토콜 호환성을 검사합니다. 클라이언트 GameTest는 원본 팩을 읽어 일반·소형 창의 5종 팝업, 긴 설명 스크롤, Enter 선택 요청, 닫힌 창의 지연 응답, 실제 키 입력 경로에서 Shift+F와 기본 F의 분리를 확인합니다. 통합 시험 서버는 국가 메뉴 프로토콜을 제공하지 않으므로 이 입력 검사는 실제 운영 서버에서 메뉴가 열리는 왕복 검증을 대신하지 않습니다. 실제 다중 접속·재접속·팩 다운로드 확인은 별도이며 운영 서버를 자동 시작하거나 재시작하지 않습니다.

## 누락 연구 이미지 수정 (2026-09-09)

신궁과 발전된/미래형 다목적 기체는 확인된 원본 그림을 연구 카드·슬롯·상세창에서 사용합니다. KOR 전용 그림은 인증된 스냅샷의 국가가 대한민국일 때만 선택하고, 다른 국가의 공통 이미지 선택은 유지합니다. 장비 상세 그림이 없으면 같은 연구의 국가별 그림을 대표 이미지로 표시합니다. 12인치 부포는 서버 연구 목록에서 제거했으며 시험용 목록도 415개로 맞췄습니다.

실제 클라이언트 GameTest의 `hoi-research-three-original-images` 화면으로 세 원본을 촬영하고 국가별 자원 선택과 상세 카드 연결을 확인합니다. 운영 서버/클라이언트 설치와 다중 접속 확인은 별도입니다.

표시 명칭은 원본 번역 파일에 따라 대한민국의 `antiair2`는 `KP-SAM 신궁`, `advanced_small_airframe`은 `발전된 미래형 다목적 기체`, `modern_small_airframe`은 `미래형 다목적 기체`입니다. `HOI/scripts/import_research_names.py`로 명시된 원본 항목을 다시 가져오며 저장 ID·비용·진행은 변경하지 않습니다.

추가로 `advanced_medium_airframe`과 `advanced_large_airframe`은 각각 `발전된 미래형 무인기체`, `발전된 미래형 대형 기체`입니다. 기체 설계용 번역 별칭에 의해 덮어써지지 않도록 연구 원본 이름을 명시적으로 가져옵니다.

외부 팩은 `resources.zip`·`resources-ui.zip`·`resources-audio.zip`·`resources-portraits.zip`으로 분리됩니다. 기본 ZIP 옆의 `resource-packs.json`을 포함한 전체 빌드 묶음을 사용합니다. 클라이언트 GameTest는 4개 ZIP을 각각 선택하며 서버가 내보낸 지도 블록 상태와 생성 팔레트도 함께 불러옵니다. 모델은 공군기지 63·해군기지 70·레이더 49·도시 100큐브, 승점은 앞뒤 면을 가진 평면입니다. 실제 운영 서버의 다운로드와 다중 접속 동기화는 이 렌더링 fixture 검증에 포함하지 않습니다.


국가 정보 국기 버튼은 원본 국기 비율에 맞춰 가로로 넓히고 TFR의 `gfx/interface/flag_overlay.dds` 원본 오버레이(98×67)를 덧씌웁니다. 82×52 국기는 프레임 안쪽에 배치하며, 광택·음영은 원본 이미지의 알파 합성을 사용합니다. 국기 버튼의 마우스 툴팁은 표시하지 않습니다. 세계 긴장도는 서버가 제공한 변화 내역 툴팁과 이력 창을 엽니다. 다른 국가 수치 설명과 키보드 접근용 버튼 이름은 유지합니다. 금융 수치 칸 폭을 조정해 일반 화면의 지표가 모두 들어가도록 했습니다.

건설 프로빈스 시설은 보급 중심지·철도, 해군기지·요새, 해안 요새 순서입니다. 철도 버튼은 서버가 제공한 선택 항목으로 두 프로빈스를 차례로 지정하며, 서버가 신규 건설·증설·취소를 처리합니다. 해군·공군 강은 각각 기본 바다와 같은 회색·밝은 회색 콘크리트 재질입니다. 새 재질의 `atlas_scene_v2`/`atlas_scene_ready_v2`는 서버와 함께 갱신해야 합니다.

분할 팩의 실제 렌더링 검사는 네 ZIP을 모두 적용하여 원본 UI·소리와 철도 아이콘, 버튼 순서, 국기 비율, 전체 강 재질의 64블록 거리 처리를 검사합니다. 운영 서버의 팩 HTTP 다운로드·다중 접속은 서버를 실행하지 않았으므로 별도 확인이 필요합니다.

프로빈스 경계는 육상·해상 모두 회색, 주·해역·공역·해안 경계는 검은색입니다. `atlas_scene_v2`/`atlas_scene_ready_v2`를 서버와 함께 사용합니다. 육상 높이는 서버 NBT에 저장되며 실행 중 지형 종류로 추가하지 않습니다. 오키나와의 겹친 연결은 두 표시 프로빈스 사이 직선 하나로 표시합니다. `AtlasSurfaceRenderChecks`는 오키나와·프놈펜·중국 내륙의 서버 출력 fixture를 실제 렌더러로 확인합니다. 운영 서버 재접속·다중 접속 검증은 별도입니다.

무역 목록은 국가·수출·운송됨·수송선을 표시하고 행을 눌러 별도 계약 창을 엽니다. 경로 불가 및 양방향 금수는 빨강, 거래 가능한 미거래 국가는 노랑, 현재 계약이 있는 국가는 초록입니다. 민간공장 아이콘과 노란색 이용 가능 수치를 표시합니다. 수송선 필요 수는 서버 경로 계산값이며 육로는 0, 알 수 없거나 경로가 없으면 —입니다. 생산 라인은 맨 위로 이동 외에 한 줄씩 위·아래 이동하며 공장 효율을 그대로 보존합니다.

장교단은 육군·해군·공군·특수부대 구역을 원본 배너와 배경으로 구분합니다. 정보기관은 창설 전 창설 버튼만, 창설 후 개선 버튼과 작전/암호학 탭을 표시하고 개선은 별도 분류 창으로 엽니다. 화면에 없는 원본의 장교·개선 효과를 임의로 추가하지 않으며 현재 서버에 등록된 정보만 표시합니다. GUI 3 (2560×1440) 렌더러 캡처를 추가했습니다. 글꼴은 Minecraft의 현재 기본 폰트 렌더러이며 HOI 전용 한글 폰트를 따로 덮어쓰지 않습니다. 사용자가 적용한 다른 리소스팩의 기본 폰트는 영향을 줄 수 있습니다.

정치 상단은 초상화와 이름을 한 칸에 배치하고 국가 중점의 제목 부분만 클릭할 수 있습니다. 국민정신은 왼쪽 정렬과 가로 휠 이동을 사용합니다. 정치 체제·다음 선거를 정당 목록 왼쪽으로 모으고, 정당 이름·색상·지지율 목록은 네 항목 제한 없이 자체 스크롤합니다. 서버가 보낸 이름만 표시하며 현재 서버의 네 이념 분류를 TFR의 11개 실제 정당으로 대체하는 것은 아직 미구현입니다. 화면 검증의 11개 정당 이름은 렌더링 fixture입니다.

첩보기관의 다섯 분류 아래에는 원본 14프레임의 흰색 네모·줄무늬를 표시합니다. 서버가 제공한 현재/최대 레벨을 사용하며, 반복 개선 항목 아래에는 최대 레벨 수만큼 원을 배치하고 완료분만 녹색으로 채웁니다. 공용 닫기 X는 버튼 중앙에 그립니다. 메뉴와 내부 화면의 호버 툴팁·노란 강조는 숨기고, 별도 요청한 소비재 설명만 유지합니다. 소비재 설명은 필요한 공장·남은 민간공장·실제 비율·보정 요인을 요약합니다.

생산 공장 ± 버튼은 Shift를 누르면 10개씩 요청하며 서버 한도를 넘지 않습니다. 구형 장비는 원본 붉은 오버레이를 사용합니다. 연구 상세는 연구 대상 장비를 먼저 표시하고 그 뒤에 해금 항목을 표시하며 기갑 비율 0.05는 초록색 +5%로 표시합니다. 실제 장비 해금·편제·배치 전체 연결은 위 미구현 범위와 구별합니다.

정치-경제 연합·세부 이념·정치 체제의 이미지 칸은 행 높이와 같은 폭의 정사각형입니다. 국가중점은 금속 배경의 원본 359:107 비율을 유지하고, 배경 왼쪽 아이콘 칸의 중심을 실제 아이콘 중심에 맞춥니다. 아이콘 크기와 위치는 유지하며 아래 세부 이념 칸과 4 GUI 단위 간격을 둡니다. 선택 버튼은 원본 배경 내부 좌표에 배치하고 선택 문구와 진행 중인 이름은 그 칸의 중앙에 표시합니다. 국민정신 칸의 왼쪽 경계는 다음 선거 칸과 같고 오른쪽 경계는 패널 안쪽 끝을 유지합니다. 다음 선거는 정치 체제 오른쪽에 제목과 날짜 두 줄로 표시합니다. 정당 원 그래프는 남은 칸 크기에 맞춰 확대하고, 원 둘레와 부채꼴 경계를 4×4 표본으로 매끄럽게 표시합니다. 그래프는 서버 비율과 정당 색상을 그대로 사용하며 표시할 값이 없는 부분은 중립색으로 유지합니다. GUI 2와 소형 창의 정사각형·인접 영역 겹침·다중 정당 그래프는 실제 렌더링 fixture로 확인합니다.

## 모병·정치·알림 화면

모병 및 배치는 증원·업그레이드·보급 트럭·작전·주둔군·습격 영역과 오른쪽 사단 편제 목록으로 나뉩니다. 서버의 훈련 대기열이 비어 있으면 아래 목록도 비어 있습니다. 훈련 버튼이 서버에 모병을 요청하고 응답받은 편제·배치 주별 묶음을 표시합니다. 묶음의 모든 라인 즉시 배치는 훈련 20% 이상인 대기열만 배치하며, 모든 라인 생산 취소는 해당 묶음의 인력·장비 예약을 반환합니다. 단일 리비전과 서버 트랜잭션을 사용하며 다른 국가나 묶음에는 적용되지 않습니다. 배치 장소는 도시 없이 주 이름으로 표시합니다. 산업 프로토콜 v3와 지도 장면 v4는 서버·클라이언트를 함께 갱신해야 합니다. 새 훈련은 장소 미지정·연속 횟수 0(∞)으로 시작합니다. −/+는 연속 횟수를 조정하고, 행에는 현재 회차/전체 횟수를 표시합니다. 배치 후 남은 회차 또는 무한 훈련은 새 인력·장비 지급을 받아 계속합니다. 장소 미지정을 좌클릭하면 기본 지도에서 서버가 허용한 프로빈스를 초록색 사선으로 표시하며, 지정한 주 이름은 흰색으로 표시합니다. 우클릭은 장소를 초기화합니다. 미지정 상태에서 완료된 훈련은 배치를 기다립니다. 편제 목록 삭제는 기존 사단과 훈련을 보존하며 목록에서 숨깁니다. 퇴역된 편제 보기 조작은 표시하지 않습니다. 접기·펼치기, 횟수 −/+, 단일 취소와 목록 삭제는 외부 리소스팩의 원본 이미지를 사용합니다.

훈련 묶음은 원본 490×84, 요약은 518×40, 개별 라인은 514×40 이미지 치수에 같은 배율을 적용합니다. 이미지 중심을 패널 중심에 맞추고 내부 아이콘·막대·버튼도 해당 이미지 좌표를 따릅니다. 묶음 높이와 스크롤 범위도 이 배율로 계산합니다. 여러 훈련 라인의 1600×1000·2560×1440 렌더링 검사는 클라이언트 fixture이며 실제 서버 모병 동기화 검증을 대신하지 않습니다.

사단 설계 옆 특수부대 아이콘은 서버의 사용 대대/수용량을 표시하며 숫자는 아이콘 오른쪽 4 GUI 단위 간격으로 붙입니다. 여섯 모병 항목의 검은 라벨 배경은 글씨 위치를 유지하면서 아래로 1 GUI 단위 보정합니다. 보급 HUD는 재고량 대신 보급 효율을 표시하며 100%는 흰색, 100% 미만은 `dark_red`(#AA0000)입니다. 값이 없으면 `—`입니다. 공장은 다섯 자리 정수를 표시하고, 안정도·전쟁 지지도는 음수 부호를 포함할 공간을 확보합니다. 군종 경험치 상한은 1000, 핵폭탄 HUD는 최대 999까지 표시합니다.

정치 화면의 정당 목록은 스크롤 없이 전체 항목을 표시합니다. 경제 모델·세력은 정사각형이고, 권력의 균형은 활성화된 서버 스냅샷이 있을 때만 점령지·협력정부 아이콘 위에 표시합니다. 옆 상세 창은 원본 양측 아이콘·범위·현재 값을 표시합니다. 정보기관 창에는 개설 전·개설 중·작전·암호학·개선 상태가 있으며 개선 레벨 원과 카테고리 네모를 구분합니다. 기술·국가중점 완료 창은 원본 500×260 비율과 세부 사항·확인 버튼을 사용합니다.

세계 긴장도 이력은 날짜·국가·세계 긴장도 정렬과 현재 전쟁 목록, 양측 전쟁 상세를 제공합니다. 이력 아이콘은 시계이며 기여도는 정수 %입니다. 서버가 보고하지 않은 손실·인력은 알 수 없음으로 남깁니다. 긴 이력은 서버 페이지와 화면 스크롤로 조회합니다.

### 검증 범위와 남은 연결

`test runGameTest remapJar`의 실제 클라이언트 렌더링 캡처는 `client/build/run/clientGameTest/screenshots/`에 생성됩니다. 캡처의 국가·편제·전쟁은 검증용 스냅샷이며 운영 서버나 실제 다중 접속 동기화 검증을 뜻하지 않습니다. 원본 자산은 외부 리소스팩에서만 읽습니다.

여섯 전역 우선순위 선택은 국가별로 저장되며, 증원은 실제 장비 충족률·부족량과 배정 우선순위에 연결됩니다. 나머지 다섯 영역의 별도 장비 배분 계산과 게이지는 아직 제공되지 않습니다. 권력의 균형은 활성 상태·값·범위 표시와 이야기의 설정/증감/제거 처리가 연결되어 있으나, 모든 원본 결정·범위 진입/이탈 효과가 실행되는 것은 아닙니다. 전쟁 손실 등 누락된 보고 데이터, 전체 TFR 사건·중점·교리의 원본 동등성은 이 UI 검증만으로 보장하지 않습니다.

이벤트 창은 2560×1440 참고 화면의 슈퍼 이벤트 약 1000×640, 글로벌·국가 이벤트 약 620×800 비율을 기준으로 조정합니다. 작은 창에서는 가독성 최소 너비를 적용하고 본문·선택지 스크롤을 유지합니다. 모병·배치와 이벤트의 2560×1440·1600×1000 참고 캡처는 GUI 배율 2를 명시적으로 설정하고 실제 GUI 크기도 절반인지 검사합니다. 파일 이름의 `-gui2`로 구분합니다. 렌더링 픽스처와 서버 요청 검사는 실제 운영 서버에 연결한 다중 사용자 검증을 대신하지 않습니다.

안정도·전쟁 지지도·정당 인기도 툴팁은 TFR 공통 효과를 반영한 서버 계산값을 사용합니다. 정당 인기도는 매일 정치력 고정 획득이며 기본 안정도를 올리지 않습니다. 공장·건설·사단 조직력 등 적용 범위와 아직 연결되지 않은 TFR 월간 재정·발전도 효과는 서버의 `docs/state-affairs/government.md`에 기록합니다.

정부·경제법·군법·사회법·발전도·군사 참모 슬롯은 서버가 발급한 후보 선택 창과 정치력 비용·조건 검증 경로에 연결됩니다. 현재 콘텐츠에는 경제 중점·무역법·징병법·군사 참모의 후보가 등록되어 있습니다. 정부의 여섯 인사 슬롯, 나머지 경제법·군법, 사회법·발전도는 후보 데이터가 없어 실제 변경을 제공하지 않습니다. 빈 창의 후보 미등록 안내는 정상이며, 원본 후보·비용·효과 전체가 구현되었다는 의미가 아닙니다.

모병 화면의 여섯 분류 아이콘과 중간 `recruitment/icon_bg` 배경은 모두 맨뒤 행 배경의 왼쪽 칸 중심을 기준으로 배치합니다. 원본 493×57 이미지에서 칸 경계 `(0, 4)..(96, 55)`와 실제 비율 유지 렌더링 좌표를 사용하며, 아이콘과 중간 배경을 함께 위로 2 GUI 단위 보정합니다. 편제 및 훈련 아이콘은 오른쪽으로, 연속 횟수 −/∞/+는 오른쪽 아래로 보정합니다. 필요 장비의 펼친 상자는 원래 좌표에서 오른쪽·아래로 각각 1 GUI 단위 보정하며, 여섯 게이지의 나무 상자는 원래 세로 좌표를 사용합니다. GUI 2에서 1 GUI 단위는 2 화면 픽셀에 해당합니다.

HOI 자체 통신 형식은 v1이며, 서버의 SaveManager/state.json만 v2입니다. 같은 변경의 서버·클라이언트 JAR을 함께 사용합니다. 건설 지도 입력은 선택 슬롯 번호 대신 실제 construction 지도 차원에서 판정하여 좌클릭으로 건설하고 우클릭으로 취소합니다. 장교단 정신 버튼은 양옆 여백을 두며, 건설 기호 버튼은 글꼴 대신 중앙 정렬 도형을 사용합니다. 정치 점령지·종속국 버튼은 서버에서 받은 상세 정보를 엽니다. 렌더링 GameTest는 실제 운영 서버 접속 검증을 대신하지 않습니다.

정치 법률 선택 창은 서버가 발급한 공통 법률 후보와 현재 적용 법률만 표시합니다. 국가 전용 법률은 현재 적용 중인 항목에 한해서만 표시하며, 교체 후에는 같은 국가에서도 목록에서 사라집니다. 다른 국가의 전용 법률과 미적용 전용 법률은 선택 불가 행으로도 표시하지 않습니다. 교체 불가 조건과 비용은 서버가 검증합니다. 법률·장관 이미지는 외부 팩의 `politics/appointments/` 경로를 사용합니다. 훈련 중 조기 배치 기준은 선택한 훈련법에 따른 서버의 `minimumTraining` 값을 사용하며, 구형 스냅샷에 값이 없을 때만 20% 기준을 유지합니다.


게임이 시작된 선택 국가의 상단 정보는 `hud_state_v1`으로 갱신됩니다. `/hoi menu`는 정치 화면을 열고 닫은 뒤에도 상단 정보는 유지됩니다. 게임 종료·국가 선택 해제·접속 종료 시 숨깁니다. 다른 HOI 화면은 공통 상단 바를 자체 표시합니다.

정보기관 메뉴는 개설 전/개설 중/개설 후 화면을 직접 엽니다. 모집·포획·사살 요원 수는 툴팁 전용이며 클릭 동작이 없습니다. 연구의 제한된 혜택과 연구 속도는 동일한 좌우 여백 안에서 양 끝으로 정렬합니다. 일반 버튼은 글씨를 1 GUI 단위 확대하고, 패널 좌측 상단 제목은 2 GUI 단위 확대합니다. 닫기 X는 외부 팩의 TFR 원본 `menu/close` 이미지를 공통 사용합니다.

사단 편제의 지원중대/대대 선택은 두 열의 아이콘 카드로 표시합니다. 새 대대는 기갑·기동·보병 분류를 거치며 현재 대대와 지원중대는 직접 목록을 엽니다. 현재 선택 상태와 제거를 구분하고 서버가 등록한 정의만 보여줍니다. 원본 화면의 비용이나 미등록 병종을 임의로 만들지 않습니다.

배치 장소를 누르면 클라이언트 모병 패널과 버튼이 숨겨지고 상단 국가 정보가 남습니다. 지도 좌클릭은 위치 요청을 보내면서 초록 사선을 지우고 모병 및 배치 메뉴를 다시 표시합니다. 우클릭은 선택을 취소하고 메뉴로 돌아옵니다. 서버의 장소 검증과 저장은 유지됩니다. 클라이언트 GameTest는 숨김·복귀·늦은 사선 재표시 차단과 실제 화면을 검사합니다. 운영 서버 다중 접속 검증은 수행하지 않습니다.

최소 글자 높이는 10 GUI이며 정보기관·정치·완료 알림에 적용합니다. 모든 패널 닫기 버튼은 `닫기`만 툴팁으로 표시하고 공석은 툴팁을 표시하지 않습니다. 정치 상세창은 중복 현재 상태 요약 없이 다른 법률 아이콘으로 바로 전환합니다. 정보기관 개선은 추가 확인창 없이 시작하며, 작전 목록은 해외 배치 후 나타나고 서버의 첩보망 조건으로 실행을 허용합니다. 작전 및 암호학 행은 아이콘·진행 상태·행별 버튼을 표시합니다. 현재 서버가 구현한 단순화 작전 3종과 각 50% 첩보망 기준을 사용하며 TFR 전체 작전의 이식은 아닙니다.

새 사단 설계는 빈 편제에서 시작합니다. 맨 위 대대 칸은 기갑·기동·보병 분류를 열며 열의 종류 변경은 기존 대대 수 × 5 경험치를 계산합니다. 변경된 편제의 총 비용은 서버에서 계산해 저장 시 차감합니다. 예상 생산 비용은 흰색과 원본 산업력 이미지로 표시합니다. 생산·건설 위/아래 버튼은 Shift를 누르면 맨 위/맨 아래로 이동하며 별도 맨 위 이동 버튼은 없습니다.

지도 비무장지대는 RGB FF0000/알파 254의 발광 선이고 육상 경계 위에 표시됩니다. 해군기지는 선택 시 수리 함선 수/현재 기지 레벨을 표시합니다. 해군·공군기지 선택은 기본 클릭음을 사용합니다. 자동 클라이언트 GameTest의 캡처와 실제 운영 서버 접속 확인은 구분하며, 이번 변경으로 운영 서버를 시작하거나 재시작하지 않았습니다.

정보기관 화면의 내부 구역명은 **첩보기관**이다. 작전 구역은 원본 `agency_header.dds`, `operatives_bg.dds`, `operations_tab_large.dds`의 두 프레임과 `operations_entry_bg.dds`를 사용한다. 요원 수치는 원본의 세 개 홈 중앙에 배치하며, 상세창은 원본 대상 국가 카드·클립보드·하단 바를 사용한다. 리소스 존재 검증과 렌더링 GameTest를 적용하며 운영 서버의 실제 화면 확인은 별도다.

첩보기관 사이드바는 GUI 2 기준 최소 360 GUI 폭으로 분류명을 한 줄로 표시한다. 요원 아이콘과 숫자는 원본 `operatives_bg.dds`의 각 수치 홈 중심(59/136/203.5 source px)을 같은 비율로 변환해 정렬한다.
`atlas-client-scene.json` 및 지역별 렌더링 fixture는 서버 GameTest의 최신 출력과 함께 갱신한다. 모든 지도 모드는 지표면에 붙인 강(.001 블록), 프로빈스 선(.012), 주·해역 선(.024) 순서를 공유한다. 육군·해군·공군·보급 지도 렌더링을 확인하며 보급 지도의 강은 바다와 같은 밝은 회색을 사용한다.

The client GameTest captures all nine original map selector icons at GUI 2 in `hoi-map-selector-icons-gui2`. The server retains the existing key bindings and explicitly disables custom-name italics. The infantry selector contains only the complete helmet frame, without the adjacent NATO symbol. This is an isolated development client check, not operational-server verification.
